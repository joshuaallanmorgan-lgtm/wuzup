<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <!-- Minimal placeholder mark (Stage C4.5 — the app shipped NO favicon after C2's
       orphan sweep). Warm CTA square + monogram; ⚑ Charles owns the real mark. -->
  <rect width="64" height="64" rx="14" fill="#bb5719"/>
  <text x="32" y="45" text-anchor="middle" font-family="system-ui, -apple-system, 'Segoe UI', sans-serif" font-size="36" font-weight="800" fill="#fefdfb">W</text>
</svg>
